import React from 'react'

/** Iron Squad emblem used as the small admin nav icon (replaces the Payload mark). */
const Icon: React.FC = () => {
  return <img alt="Залізна Зміна" src="/logo-iron-squad.svg" style={{ height: 26, width: 'auto' }} />
}

export default Icon
